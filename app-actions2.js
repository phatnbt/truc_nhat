function updateBill(kind,value){if(!requireAdmin())return renderBilling();const b=ensureBill();if(b.closed)return toast("Tháng đã chốt sổ");b[kind]=Math.max(0,Math.round(Number(value)||0));b.updatedAt=nowIso();persist("Đã cập nhật hóa đơn",{action:"UPDATE_BILL",summary:`Cập nhật ${kind==="electricity"?"tiền điện":"tiền nước"} ${monthLabel(b.month)}`});}
function setBillMonth(value){if(!/^\d{4}-\d{2}$/.test(value||""))return;state.billing.selectedMonth=value;ui.selectedMonth=value;ui.activeBillPersonId=currentBill()?.people?.[0]?.id||null;saveLocal();renderAll();}
function syncBillingMembers(){if(!requireAdmin())return;const b=ensureBill();if(b.closed)return toast("Tháng đã chốt sổ");const n=monthDays(b.month),all=Object.fromEntries(Array.from({length:n},(_,i)=>[String(i+1),true])),norm=v=>String(v||"").trim().replace(/\s+/g," ").toLowerCase();let added=0,relinked=0;state.members.forEach(m=>{let person=b.people.find(p=>p.memberId===m.id);if(!person){const sameName=b.people.filter(p=>!p.memberId&&norm(p.name)===norm(m.name));if(sameName.length===1){person=sameName[0];person.memberId=m.id;person.name=m.name;person.updatedAt=nowIso();relinked++;}else{b.people.push({id:uid(),memberId:m.id,name:m.name,days:{...all},paid:false,paidAmount:0,updatedAt:nowIso()});added++;}}else if(person.name!==m.name){person.name=m.name;person.updatedAt=nowIso();}});b.updatedAt=nowIso();ui.activeBillPersonId=ui.activeBillPersonId||b.people[0]?.id||null;const parts=[];if(relinked)parts.push(`đã liên kết lại ${relinked} người`);if(added)parts.push(`đã thêm ${added} thành viên`);persist(parts.length?parts.join(" · "):"Danh sách đã đầy đủ",{action:"SYNC_BILL_MEMBERS",summary:`Đồng bộ thành viên vào ${monthLabel(b.month)}${relinked?` · liên kết lại ${relinked}`:""}${added?` · thêm ${added}`:""}`});}
function addBillingPerson(){if(!requireAdmin())return;const name=$("#billNewPersonName").value.trim().replace(/\s+/g," "),b=ensureBill();if(!name)return toast("Nhập tên người");if(b.closed)return toast("Tháng đã chốt sổ");if(b.people.some(p=>p.name.toLowerCase()===name.toLowerCase()))return toast("Tên này đã có");const p={id:uid(),memberId:null,name,days:{},paid:false,paidAmount:0,updatedAt:nowIso()};b.people.push(p);b.updatedAt=nowIso();ui.activeBillPersonId=p.id;$("#billNewPersonName").value="";closeModal("memberModal");persist("Đã thêm người/khách",{action:"ADD_BILL_PERSON",summary:`Thêm ${name} vào ${monthLabel(b.month)}`});}
function selectBillPerson(personId){ui.activeBillPersonId=personId;saveLocal();renderBilling();}
function removeBillingPerson(){if(!requireAdmin())return;const b=currentBill(),p=activeBillPerson(b);if(!b||!p)return;if(b.closed)return toast("Tháng đã chốt sổ");if(!confirm(`Xóa ${p.name} khỏi bảng tháng này?`))return;b.people=b.people.filter(x=>x.id!==p.id);b.updatedAt=nowIso();ui.activeBillPersonId=b.people[0]?.id||null;persist("Đã xóa khỏi bảng tháng",{action:"REMOVE_BILL_PERSON",summary:`Xóa ${p.name} khỏi ${monthLabel(b.month)}`,targetMemberId:p.memberId||null});}
function setBillDay(day){const b=currentBill(),p=activeBillPerson(b);if(!p)return toast("Chọn người trước");if(!canEditBillingPerson(p))return toast("Bạn chỉ được chỉnh ngày ở của chính mình");if(b.closed)return toast("Tháng đã chốt sổ");p.days[String(day)]=!p.days?.[String(day)];p.updatedAt=nowIso();b.updatedAt=nowIso();persist(p.days[String(day)]?`Ngày ${day}: Có ở`:`Ngày ${day}: Vắng`,{action:"UPDATE_BILL_DAY",summary:`${p.name} ngày ${day}: ${p.days[String(day)]?"Có ở":"Vắng"}`,targetMemberId:p.memberId||null});}
function dateDay(value,bill){if(!value)return null;const [y,m,d]=value.split("-").map(Number),[by,bm]=bill.month.split("-").map(Number);return y===by&&m===bm?d:null;}
function applyStayRange(value){const b=currentBill(),p=activeBillPerson(b);if(!p)return toast("Chọn người trước");if(!canEditBillingPerson(p))return toast("Bạn chỉ được chỉnh ngày ở của mình");if(b.closed)return toast("Tháng đã chốt sổ");let from=dateDay($("#stayFrom").value,b),to=dateDay($("#stayTo").value,b);if(!from||!to)return toast("Chọn đủ khoảng ngày");if(from>to)[from,to]=[to,from];if(!value&&!confirm(`Đánh dấu vắng từ ngày ${from} đến ${to}?`))return;for(let d=from;d<=to;d++)p.days[String(d)]=value;p.updatedAt=nowIso();b.updatedAt=nowIso();persist(value?`Đã đánh dấu ở ngày ${from}–${to}`:`Đã đánh dấu vắng ngày ${from}–${to}`,{action:"UPDATE_BILL_RANGE",summary:`${p.name}: ${value?"Có ở":"Vắng"} ngày ${from}–${to}`,targetMemberId:p.memberId||null});}
function presetStay(mode){const b=currentBill(),p=activeBillPerson(b);if(!p)return toast("Chọn người trước");if(!canEditBillingPerson(p))return toast("Bạn chỉ được chỉnh ngày ở của mình");if(b.closed)return toast("Tháng đã chốt sổ");if(mode==="none"&&!confirm("Bỏ toàn bộ ngày ở?"))return;const [y,m]=b.month.split("-").map(Number),n=monthDays(b.month);p.days={};for(let d=1;d<=n;d++){const dow=new Date(y,m-1,d).getDay();p.days[String(d)]=mode==="all"||(mode==="weekdays"&&dow!==0&&dow!==6);}p.updatedAt=nowIso();b.updatedAt=nowIso();persist("Đã cập nhật nhanh ngày ở",{action:"PRESET_BILL_DAYS",summary:`${p.name}: cập nhật nhanh ngày ở ${monthLabel(b.month)}`,targetMemberId:p.memberId||null});}
function toggleBillClosed(){if(!requireAdmin())return;const b=currentBill();if(!b)return toast("Chưa có dữ liệu tháng");if(!b.closed){const c=billCalc(b);if(!c.totalDays)return toast("Chưa có ngày ở để chốt");if(!confirm("Chốt sổ và khóa chỉnh sửa ngày ở/hóa đơn?"))return;b.closed=true;}else{if(!confirm("Mở khóa tháng để chỉnh sửa?"))return;b.closed=false;}b.updatedAt=nowIso();persist(b.closed?"Đã chốt sổ":"Đã mở khóa sổ",{action:b.closed?"CLOSE_BILL":"OPEN_BILL",summary:`${b.closed?"Chốt sổ":"Mở khóa"} ${monthLabel(b.month)}`});}
function togglePayment(personId){if(!requireAdmin())return;const b=currentBill(),p=b?.people?.find(x=>x.id===personId);if(!b||!p)return;if(!b.closed)return toast("Hãy chốt sổ trước khi ghi nhận thanh toán");const c=billCalc(b),due=c.due[p.id]||0,wasPaid=isBillPaid(p,due);p.paid=!wasPaid;p.paidAt=p.paid?nowIso():null;p.paidAmount=p.paid?due:0;p.paidBy=p.paid?authSession.user?.uid||"admin":null;p.updatedAt=nowIso();b.updatedAt=nowIso();persist(p.paid?`Đã ghi nhận ${p.name} đã đóng tiền`:`Đã chuyển ${p.name} về chưa đóng`,{action:p.paid?"MARK_BILL_PAID":"MARK_BILL_UNPAID",summary:`${p.name}: ${p.paid?"Đã đóng":"Chưa đóng"} ${money(due)}`,targetMemberId:p.memberId||null});}
function billingText(){const c=billCalc();return [`⚡💧 ĐIỆN NƯỚC ${monthLabel(c.b.month).toUpperCase()}`,`Tổng: ${money(c.b.electricity+c.b.water)}`,"",...c.people.map(p=>`• ${p.name}: ${stayCount(p,c.b)} ngày · ${money(c.due[p.id])} · ${isBillPaid(p,c.due[p.id]||0)?"ĐÃ ĐÓNG":"CHƯA ĐÓNG"}`)].join("\n");}
function downloadBillingCsv(){const c=billCalc();const rows=[["Tên","Ngày ở","Tiền điện","Tiền nước","Tổng","Thanh toán","Đã thu"],...c.people.map(p=>[p.name,stayCount(p,c.b),c.electric[p.id],c.water[p.id],c.due[p.id],isBillPaid(p,c.due[p.id]||0)?"Đã đóng":"Chưa đóng",p.paidAmount||0]),["TỔNG",c.totalDays,c.b.electricity,c.b.water,c.b.electricity+c.b.water,"",c.people.reduce((s,p)=>s+(p.paidAmount||0),0)]];const csv="\ufeff"+rows.map(r=>r.map(v=>`"${String(v).replaceAll('"','""')}"`).join(",")).join("\n");const a=document.createElement("a");a.href=URL.createObjectURL(new Blob([csv],{type:"text/csv;charset=utf-8"}));a.download=`dien-nuoc-${c.b.month}.csv`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000);toast("Đã tải CSV");}

function auditTimeMs(log){try{if(log?.createdAt?.toDate)return log.createdAt.toDate().getTime();const n=new Date(log?.createdAt||0).getTime();return Number.isFinite(n)?n:0;}catch{return 0;}}
function auditActorKey(log){return String(log?.actorUid||log?.actorEmail||log?.actorName||"unknown").toLowerCase();}
function auditActorName(log){return log?.actorName||log?.actorEmail||"Không xác định";}
function auditActionLabel(action){const labels={UPDATE_DATA:"Cập nhật dữ liệu",ADD_MEMBER:"Thêm thành viên",REMOVE_MEMBER:"Xóa thành viên",UPDATE_PRESENCE:"Cập nhật có mặt",SET_ALL_PRESENCE:"Cập nhật toàn phòng",CREATE_SCHEDULE:"Tạo lịch",UPDATE_SCHEDULE:"Sửa phân công",UPDATE_TASK:"Cập nhật công việc",SUBMIT_TASK:"Báo hoàn thành",VERIFY_TASK:"Xác nhận công việc",REJECT_TASK:"Yêu cầu làm lại",DELETE_SCHEDULE:"Xóa lịch",UPDATE_BILL:"Cập nhật hóa đơn",SYNC_BILL_MEMBERS:"Đồng bộ thành viên",ADD_BILL_PERSON:"Thêm người hóa đơn",REMOVE_BILL_PERSON:"Xóa người hóa đơn",UPDATE_BILL_DAY:"Sửa ngày ở",UPDATE_BILL_RANGE:"Sửa khoảng ngày",PRESET_BILL_DAYS:"Cập nhật nhanh ngày ở",CLOSE_BILL:"Chốt sổ",OPEN_BILL:"Mở khóa",MARK_BILL_PAID:"Đã thu tiền",MARK_BILL_UNPAID:"Hủy thu tiền",APPROVE_ACCESS:"Duyệt tài khoản",UPDATE_ACCESS:"Sửa quyền",REVOKE_ACCESS:"Khóa tài khoản",DELETE_ACCOUNT:"Xóa hoàn toàn tài khoản",CLEANUP_AUDIT:"Dọn nhật ký"};return labels[action]||action||"Cập nhật";}
function filteredLogs(){const q=auditFilters.query.trim().toLowerCase();return [...auditLogs].sort((a,b)=>auditTimeMs(b)-auditTimeMs(a)).filter(log=>{if(auditFilters.member!=="all"&&auditActorKey(log)!==auditFilters.member)return false;if(auditFilters.action!=="all"&&(log.action||"UPDATE_DATA")!==auditFilters.action)return false;if(!q)return true;return [log.summary,log.action,auditActorName(log),log.actorEmail].some(v=>String(v||"").toLowerCase().includes(q));});}
